import { PasswordResetForm } from '@/features/auth';

const PasswordResetPage = () => {
  return (
    <div className="mt-8">
      <PasswordResetForm />
    </div>
  );
};

export default PasswordResetPage;
